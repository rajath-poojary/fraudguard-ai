import Link from "next/link";
import { useEffect, useState } from "react";
import AppShell from "../components/AppShell";
import { EmptyState, ErrorState, LoadingState, PageHeader, RiskBadge } from "../components/UI";
import { Alert, api } from "../services/api";

export default function Alerts() { const [items, setItems] = useState<Alert[] | null>(null); const [error, setError] = useState(""); useEffect(() => { api.alerts(100).then((data) => setItems(data.items)).catch((err) => setError(err.message)); }, []); return <AppShell><section className="page"><PageHeader eyebrow="Monitoring / queue" title="Fraud alerts" description="Signals requiring a closer look." />{error ? <ErrorState message={error} /> : !items ? <LoadingState /> : <div className="panel table-panel">{items.length ? <div className="table-wrap"><table><thead><tr><th>Alert</th><th>Transaction</th><th>Risk score</th><th>Reason code</th><th>Status</th><th /></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><span className="mono">{item.id.slice(0, 12)}</span><small>{new Date(item.created_at).toLocaleString()}</small></td><td><Link className="text-link" href={`/transactions/${item.transaction_id}`}>{item.transaction_id.slice(0, 12)} ↗</Link></td><td className="alert-score">{Number(item.risk_score).toFixed(1)}</td><td>{item.reason_code}</td><td><RiskBadge value={item.status} /></td><td><Link className="row-link" href={`/alerts/${item.id}`}>↗</Link></td></tr>)}</tbody></table></div> : <EmptyState title="Alert queue is clear" detail="New elevated-risk transactions will appear here." />}</div>}</section></AppShell>; }
